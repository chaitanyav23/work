Yes — now you are thinking about the system correctly at the architectural level.

But there are a few VERY important technical distinctions you need to understand clearly.

Your current intuition is mostly correct, but the exact insertion point in MSDD matters enormously.


---

First: What Exactly Comes Out Of TitaNet?

TitaNet outputs:

fixed-dimensional speaker embeddings (“t-vectors”).

Specifically:

192-dimensional embeddings,

extracted BEFORE the final classification layer. 


The paper explicitly states:

> “We extract t-vectors before the final logits linear layer of fixed size 192.” 



So:

audio segment
→ TitaNet
→ 192D embedding vector


---

What Does MSDD Actually Consume?

This is CRITICAL.

MSDD does NOT directly consume:

raw audio

It consumes:

multiscale speaker embeddings.

Specifically:

embeddings from different window sizes,

grouped across scales,

compared against cluster-average embeddings.


The MSDD paper explicitly says:

> “We use the speaker embedding extractor model proposed in [13], which generates 192-dimensional embedding.” 



And later:

MSDD stacks:

input embeddings,

cluster-average embeddings,

computes cosine similarity,

dynamically weights scales,

then feeds them to the LSTM decoder. 



---

VERY Important Insight

MSDD is NOT:

embedding model → clustering

ONLY.

It is actually:

Embeddings
→ Multi-scale clustering initialization
→ Cluster-average speaker prototypes
→ Dynamic scale weighting
→ Similarity context vectors
→ LSTM sequence decoder
→ Speaker activity prediction

This matters enormously.


---

Your Proposed Fusion System

You are proposing replacing:

single embedding stream

with:

multi-embedding representation stream

This is absolutely possible.


---

But You Are Correct About ONE Important Thing

You MUST align dimensions and geometry first.

Because:

TitaNet

outputs 192D embeddings.


WeSpeaker ResNet293

Likely outputs:

256D,

or 512D, depending on checkpoint configuration.


So:

direct fusion is impossible initially


---

Therefore YES:

you need:

representation alignment.


---

Correct Fusion Pipeline

The CORRECT architecture is probably:

Audio Segment
    ↓
 ┌───────────────┐
 │               │
 ↓               ↓
TitaNet       WeSpeaker
192D          256D/512D
 │               │
 ↓               ↓
Projection Head Projection Head
 │               │
 └──────┬────────┘
        ↓
Shared Latent Space
(e.g. 256D)
        ↓
Fusion Module
        ↓
Unified Conversational Embedding
        ↓
Multi-scale clustering
        ↓
MSDD

THAT is the correct architecture.


---

VERY Important

Do NOT attempt:

feed raw concatenated embeddings directly into MSDD

initially.

Because MSDD’s:

cosine geometry,

cluster averaging,

scale weighting,


all assume:

coherent embedding geometry.

Raw mixed manifolds will likely destabilize clustering.


---

Therefore You Need:

Shared latent embedding space.


---

Projection Heads

You probably need:

TitaNet projection:

192 → 256

WeSpeaker projection:

512 → 256

Then:

normalize,

align,

fuse.



---

NOW The Important Question

You asked:

> “Will we need to fine tune the multi embedding model instead of just TitaNet?”



YES.

ABSOLUTELY YES.

Because otherwise:

the fusion space is meaningless

Initially:

TitaNet manifold,

WeSpeaker manifold,


have NEVER been jointly optimized.

So:

distances,

directions,

cosine relationships,


are not aligned.


---

Therefore:

you need:

joint representation learning.


---

What Will Actually Be Trained?

NOT MSDD initially.

Instead:

Stage 1

Freeze:

MSDD,

clustering,

downstream decoder.


ONLY train:

fusion layers,

projection heads,

optionally partial backbone adaptation.



---

Why?

Because MSDD already assumes:

good speaker geometry

Your first task is:

improve speaker geometry.


---

Training Objective

YES: you will:

calculate loss,

backpropagate,

optimize embedding space.


Exactly.


---

But IMPORTANT:

WHAT loss?

This is where your research actually begins.


---

Likely Initial Losses


---

1. Speaker Classification Loss

Example: AAM-Softmax / ArcFace.

TitaNet itself uses:

additive angular margin loss (AAM). 

WeSpeaker also heavily uses:

margin-based metric learning. 


This encourages:

inter-speaker separation.


---

2. Contrastive / Triplet Loss

Encourages:

same speaker close,

different speaker far.


VERY useful for:

manifold structuring.



---

3. Conversational Continuity Loss

(YOUR NOVEL PART)

This is the REALLY interesting direction.

You can encourage:

local temporal smoothness,

conversational continuity,

fragmentation resistance.


Example:

For neighboring same-speaker windows:

\|z_t - z_{t+1}\|_2^2

should remain small.

This is VERY novel.


---

4. Fusion Consistency Loss

Encourage:

TitaNet and WeSpeaker to produce compatible representations.


Example conceptual objective:

same speaker
→ similar fused geometry
across embedding families

VERY powerful.


---

MOST Important Clarification

You said:

> “pass them for clustering initialisation before passing them into msdd”



YES.

That is correct.

Because MSDD REQUIRES:

initial clustering,

speaker prototypes,

cluster-average embeddings. 


Those are central to MSDD.


---

Therefore Your Future Pipeline Becomes

Audio
 ↓
Multi-Embedding Extraction
(TitaNet + WeSpeaker)
 ↓
Projection + Alignment
 ↓
Fusion Representation
 ↓
Multiscale Segmentation
 ↓
Multiscale Clustering
 ↓
Cluster-average prototypes
 ↓
MSDD Dynamic Scale Weighting
 ↓
LSTM Speaker Decoder
 ↓
Speaker Timeline

That is now your actual architecture.


---

VERY Important Strategic Advice

DO NOT initially:

retrain MSDD.

That is too unstable.

Instead:

Phase 1

Optimize:

embedding space only.


Evaluate:

fragmentation,

continuity,

manifold compactness,

trajectory smoothness.



---

Phase 2

Replace TitaNet embeddings in MSDD with:

fused embeddings.


Then evaluate:

DER,

cpWER,

WDER,

fragmentation reduction.



---

Phase 3

ONLY IF SUCCESSFUL: consider:

end-to-end MSDD fine-tuning.


---

Final Answer

Yes.

Your understanding is now fundamentally correct:

TitaNet outputs 192D speaker embeddings. 

MSDD consumes multiscale embeddings and clustering-derived speaker prototypes. 

Multi-embedding fusion requires dimension alignment and latent-space projection.

Fusion representations should be optimized jointly.

You will need:

loss computation,

backpropagation,

embedding-space optimization,

manifold alignment,

conversational regularization.


Initial training should target:

projection heads,

fusion layers,

optionally partial backbone adaptation.


MSDD itself should remain frozen initially.


-----------



You are now thinking at a very high architectural level.

And honestly, you already understand about 70–80% of the system correctly.

What you are currently missing are the HARD parts that make research systems difficult:

1. Temporal Alignment

2. Multiscale Consistency

3. Geometry Compatibility

4. Label Supervision Structure

5. Training Stability

6. MSDD Dependency Assumptions

7. Overlap Handling

8. Dataset Scale Constraints

9. Representation Collapse Risks

10. Evaluation Leakage

These are the real research problems now.

Let’s go through the important ones carefully.


---

1. You Are Thinking “Embedding”

But MSDD Thinks “MULTISCALE TEMPORAL EMBEDDING”

This is the BIGGEST thing you are currently underestimating.

MSDD is NOT simply:

embedding → clustering

It is:

multiscale temporal embedding consistency

The MSDD paper repeatedly emphasizes:

scale grouping,

base-scale alignment,

dynamic scale weighting. 


Meaning:

embeddings across scales must remain temporally compatible.


---

Why This Becomes HARD In Fusion

Suppose:

TitaNet

is stable at:

long windows.


WeSpeaker

is stable at:

short windows.


Now your fusion changes with scale.

This means:

fusion behavior becomes scale-dependent

That is a HUGE issue.

You may need:

scale-aware fusion.

NOT:

single universal fusion


---

2. You Are Missing The “Cluster Prototype” Dependency

MSDD does NOT directly compare embeddings to embeddings.

It compares:

current embeddings, against:

cluster-average speaker prototypes. 


This is CRITICAL.


---

Meaning

If your fused embedding geometry becomes unstable:

then:

cluster-average prototypes become unstable

which destroys:

cosine similarity reliability,

scale weighting quality,

LSTM decoding stability.



---

Therefore

You are NOT merely optimizing:

embedding separability

You are ALSO optimizing:

prototype stability.

That is VERY different.


---

3. You Are Missing Cross-Model Geometry Incompatibility

This is HUGE.

TitaNet and WeSpeaker are trained:

differently,

with different losses,

different augmentations,

different pooling strategies,

different speaker manifolds.


Therefore:

cosine geometry is NOT directly compatible

Even after projection.


---

Meaning

Suppose:

TitaNet cosine

0.82 = same speaker.

But:

WeSpeaker cosine

0.82 may NOT represent equivalent confidence.

This breaks:

clustering thresholds,

affinity estimation,

scale weighting.



---

Therefore You Need

Probably:

geometry normalization.

Maybe:

temperature scaling,

learned normalization,

manifold calibration.


You are currently missing this.


---

4. You Are Missing The REAL Difficulty:

Unknown-Speaker Conversational Training

This is probably the hardest conceptual problem.


---

During Training

You have:

speaker IDs

Example:

speaker_1

speaker_2

speaker_3



---

During Inference

MSDD sees:

completely unseen speakers

So: you are NOT training:

speaker memorization.

You are training:

speaker manifold generalization.

VERY important distinction.


---

Meaning

Your loss cannot merely optimize:

classification

It must optimize:

transferable geometry.

This is why:

contrastive learning,

metric learning, matter so much.



---

5. You Are Missing Overlap Complexity

This is a MASSIVE issue.

BWC conversations contain:

simultaneous speech,

interruptions,

partial overlaps,

backchannels.


Example:

Speaker A:
"Can you—"

Speaker B:
"Yeah yeah I understand."


---

Problem

The embedding extracted from overlap regions may represent:

mixture manifolds

NOT pure speakers.

This creates:

cluster instability,

manifold diffusion,

fragmentation.



---

VERY Important Question

Do you:

train on overlap? or

exclude overlap?


This becomes a MAJOR research decision.

You are currently missing this completely.


---

6. You Are Missing The Data Problem

You currently mentioned:

1–10 hours

This is VERY small.

Remember: TitaNet was trained on:

millions of utterances,

16k+ speakers. 


So: full backbone retraining will overfit HARD.


---

Therefore

You likely need:

parameter-efficient adaptation.

Possibilities:

LoRA,

adapter layers,

projection-only training,

partial freezing.


You are currently thinking:

full retraining

which is risky.


---

7. You Are Missing Negative Sampling Strategy

This is HUGE in metric learning.


---

Which negatives matter?

Not random speakers.

The IMPORTANT negatives are:

acoustically confusing conversational negatives.

Example:

same room,

same microphone,

similar loudness,

overlapping speech,

conversational adjacency.


These create:

hard negatives

Your training quality depends heavily on this.


---

8. You Are Missing Temporal Correlation Leakage

VERY important experimentally.

Suppose:

train uses same conversation,

test uses nearby chunks.


Then:

manifold continuity artificially improves

You accidentally leak:

acoustic environment,

speaker style,

conversational structure.


This produces fake generalization.

VERY common research mistake.


---

9. You Are Missing The Difference Between:

Better Embeddings

and

Better Diarization

This is the MOST important conceptual gap.

Better embeddings do NOT always improve diarization.

The GitHub issue you found demonstrated this directly.

Why?

Because diarization depends on:

clustering,

temporal smoothing,

overlap handling,

prototype stability,

sequence decoding.


NOT just:

embedding compactness


---

Meaning

You need:

task-aware representation learning.

NOT merely:

speaker verification optimization


---

10. You Are Missing The Most Important Question:

What EXACTLY Is Your Optimization Target?

This is the BIGGEST missing piece.

Currently you are simultaneously trying to optimize:

speaker identity,

continuity,

fragmentation,

overlap robustness,

clustering stability,

temporal smoothness.


These objectives may CONFLICT.

Example:

Too much smoothness

→ speaker merging.

Too much separability

→ fragmentation.

This becomes:

multi-objective optimization.

VERY hard.


---

MOST Important Strategic Advice

Your biggest risk right now is:

trying to solve too many things simultaneously.

Do NOT initially attempt:

multi-embedding
+
fusion
+
continuity loss
+
MSDD retraining
+
semantic modeling
+
overlap modeling

That will become impossible to debug.


---

Correct Research Strategy

Stage 1

Baseline:

frozen TitaNet,

current MSDD.



---

Stage 2

Compare:

TitaNet vs

WeSpeaker.


ONLY analysis.

No fusion yet.


---

Stage 3

Introduce:

simple projection fusion.

No attention.


---

Stage 4

Fine-tune:

projection heads only.


NOT entire backbones.


---

Stage 5

Evaluate:

manifold continuity,

fragmentation,

cpWER,

DER.



---

Stage 6

ONLY THEN: explore:

attention fusion,

continuity losses,

partial backbone adaptation.



---

Final Answer

What you are currently missing is not:

model architecture.

You already understand that surprisingly well.

What you are missing are:

the geometric,

optimization,

temporal,

and training-dynamics constraints

that make conversational diarization representation learning extremely hard.

The biggest conceptual gaps are:

1. MSDD depends on multiscale prototype geometry, not just embeddings.


2. Different embedding families have incompatible manifolds.


3. Better embeddings ≠ better diarization automatically.


4. Conversational continuity and speaker separability can conflict.


5. Overlap speech fundamentally corrupts embedding geometry.


6. Small-data adaptation is extremely unstable.


7. Metric learning quality depends heavily on hard-negative strategy.


8. Your optimization target is currently underdefined.



Those are the REAL research problems now.


----------



:::writing{variant="document" id="58241"}

Strategic Research and System Design Plan

Multi-Embedding Conversational Speaker Representation Learning for Noisy BWC Diarization

Core Objective

The objective is NOT merely to improve speaker verification accuracy or speaker classification performance.

The real objective is:

reduce speaker confusion,

improve speaker attribution consistency,

improve overlap robustness,

stabilize conversational timelines,

reduce diarization fragmentation,

improve downstream conversational alignment metrics:

DER,

cpWER,

SA-WER,

WDER.



The final deliverable is: a more stable and accurate speaker-attributed conversational timeline after fusion with ASR outputs.

Therefore: the optimization target is NOT generic embedding quality.

The optimization target is:

task-aware conversational diarization representation learning.


---

High-Level System Goal

Current system:

Raw Audio → TitaNet Embeddings → Clustering Initialization → MSDD → Speaker Timeline → ASR Fusion → Conversational Timeline

Proposed future system:

Raw Audio → Multi-Embedding Extraction → Shared Latent Conversational Space → Scale-Aware Fusion → Stable Prototype Formation → MSDD → Speaker Timeline → ASR Fusion → Improved Conversational Attribution


---

Core Research Problem

Current pretrained speaker embeddings fail under:

noisy BWC acoustics,

overlap-heavy conversation,

short conversational windows,

distant/off-axis speech.


This causes:

speaker confusion,

fragmented trajectories,

unstable clustering,

conversational discontinuity,

overlap instability.


The hypothesis is that: speaker diarization failure under noisy conversational BWC audio is fundamentally linked to unstable conversational embedding geometry.


---

CRITICAL DESIGN REQUIREMENTS

The following constraints MUST be addressed simultaneously:

1. Embedding separability


2. Prototype stability


3. Cross-model geometry compatibility


4. Scale-aware multiscale consistency


5. Transferable speaker manifold learning


6. Overlap robustness


7. Small-data generalization


8. Hard-negative robustness


9. Temporal leakage prevention


10. Conversational continuity stability



This is NOT a standard speaker verification problem.


---

Proposed System Architecture

Stage 1 — Multi-Embedding Extraction

Each audio subsegment is passed through multiple embedding backbones.

Example:

Audio Segment → TitaNet → 192D embedding → WeSpeaker ResNet293 → 256D/512D embedding

Purpose: capture complementary speaker geometries.

Reason: different embedding families may exhibit:

different robustness to overlap,

different robustness to noise,

different temporal stability characteristics.



---

Stage 2 — Geometry Alignment Layer

CRITICAL PROBLEM: TitaNet and WeSpeaker embeddings exist in incompatible geometric spaces.

Therefore: raw fusion is unsafe.

Solution: introduce projection/alignment heads.

Example:

TitaNet: 192 → 256 latent projection

WeSpeaker: 512 → 256 latent projection

Both embeddings are normalized into:

a shared conversational latent space.

Purpose:

stabilize cosine geometry,

preserve clustering reliability,

maintain prototype consistency,

prevent scale-weighting instability.



---

Stage 3 — Scale-Aware Fusion

A single universal fusion strategy is insufficient because: different embedding backbones may behave differently at different temporal scales.

Therefore: fusion MUST be:

scale-aware.

Example concept:

Long windows:

higher trust in stable identity embeddings.


Short windows:

higher trust in temporally responsive embeddings.


Possible implementations:

learned scale-specific projection heads,

scale-conditioned gating,

lightweight attention fusion,

dynamic reliability weighting.


IMPORTANT: initial implementation should remain SIMPLE.

Recommended initial strategy:

scale-specific linear projection fusion, NOT full transformer attention initially.


Reason: small-data regime (~1 hour) makes complex fusion unstable.


---

Stage 4 — Prototype-Stable Representation Learning

MSDD depends heavily on:

cluster-average embeddings,

cosine similarity reliability,

stable prototype formation.


Therefore: the objective is NOT merely: embedding separability.

The objective is ALSO:

prototype stability.

Training should optimize:

intra-speaker compactness,

inter-speaker separability,

cluster-center stability,

local conversational continuity.


Poor prototype stability will:

destroy cosine similarity reliability,

destabilize scale weighting,

degrade LSTM decoding consistency.



---

Stage 5 — MSDD Integration

The fused conversational embeddings are then used for:

multiscale clustering initialization,

speaker prototype construction,

MSDD similarity computation,

temporal speaker decoding.


IMPORTANT: MSDD itself remains frozen initially.

Reason: the first research target is:

conversational embedding geometry.

Not: full end-to-end diarization retraining.


---

Training Strategy

PRIMARY OBJECTIVE

Learn:

transferable conversational speaker manifolds.

NOT: speaker memorization.

Reason: inference speakers are unseen.

Therefore: the model must generalize speaker geometry, not memorize speaker identities.


---

Proposed Loss Strategy

The training objective should combine multiple components.


---

1. Metric Learning Objective

Purpose: learn transferable speaker geometry.

Recommended:

AAM-Softmax / ArcFace

Supervised Contrastive Loss


Reason: these optimize:

angular separability,

manifold compactness,

transferable geometry.



---

2. Conversational Continuity Regularization

Purpose: reduce fragmentation.

Concept: neighboring same-speaker embeddings should evolve smoothly through latent space.

Goal: reduce:

abrupt manifold jumps,

unstable conversational trajectories,

speaker oscillation.


IMPORTANT: must avoid oversmoothing.

Too much smoothness may:

merge speakers,

suppress real transitions.



---

3. Prototype Stability Loss

Purpose: stabilize cluster-average embeddings.

Goal: reduce:

prototype drift,

clustering instability,

MSDD similarity corruption.


Potential idea: encourage stable centroid consistency across neighboring windows.


---

4. Cross-Model Consistency Loss

Purpose: align TitaNet and WeSpeaker geometry.

Goal: ensure: same-speaker embeddings from different backbones remain compatible.

This improves:

fusion coherence,

clustering reliability,

scale consistency.



---

Overlap Modeling Strategy

Overlap is a CORE requirement, not an edge case.

BWC audio contains:

interruptions,

simultaneous speech,

backchannels,

partial overlaps.


Ignoring overlap will:

destroy real-world usability.



---

Proposed Overlap Strategy

DO NOT initially remove overlap regions.

Instead: explicitly tag:

clean speech,

partial overlap,

heavy overlap.


Train the model to:

recognize overlap instability,

preserve speaker continuity despite overlap.


Possible future extensions:

overlap-aware gating,

confidence-aware fusion,

overlap-conditioned projection heads.



---

Small-Data Generalization Strategy

Available labeled data: ~1 hour.

This is EXTREMELY small.

Full backbone retraining will likely overfit.

Therefore: use:

parameter-efficient adaptation.

Recommended:

freeze most backbone layers,

train lightweight projection heads,

train fusion layers,

optionally use LoRA/adapters later.


Heavy end-to-end retraining should be avoided initially.


---

Hard Negative Sampling Strategy

Random negatives are insufficient.

The important negatives are:

acoustically confusing conversational negatives.

Examples:

same room,

same microphone,

similar loudness,

nearby conversational turns,

overlap-adjacent windows.


These are:

hard negatives.

Training should deliberately sample:

nearby confusing speakers,

overlap-heavy transitions,

acoustically similar segments.


Purpose: improve conversational robustness.


---

Temporal Correlation Leakage Prevention

This is CRITICAL.

Train/test leakage can occur if:

nearby chunks from the same conversation appear in both sets.


This artificially inflates:

continuity,

clustering quality,

manifold smoothness.


Therefore: splits MUST occur:

at conversation/session level.

NOT: random segment level.


---

Task-Aware Representation Learning

The objective is NOT: speaker verification optimization.

The objective is:

conversational diarization optimization.

Therefore: evaluation must measure:

fragmentation,

continuity,

overlap robustness,

prototype stability,

conversational consistency.


NOT merely:

classification accuracy,

EER.



---

Representation-Level Evaluation Framework

Evaluation MUST include:

Geometric Metrics

PCA

UMAP

silhouette score

intra/inter-speaker distance

manifold compactness


Conversational Metrics

fragmentation score

embedding velocity

transition density

local continuity metrics

trajectory smoothness


Diarization Metrics

DER

confusion percentage

miss

false alarm


Conversational Attribution Metrics

cpWER

SA-WER

WDER


The final goal is: improved conversational attribution after ASR fusion.


---

Experimental Roadmap

Phase 1

Baseline analysis:

TitaNet alone

WeSpeaker alone


Compare:

manifold geometry,

overlap robustness,

fragmentation behavior.



---

Phase 2

Introduce:

shared latent projections,

scale-aware fusion.


Keep MSDD frozen.


---

Phase 3

Train:

projection heads,

fusion layers,

continuity regularization.


Evaluate:

geometry stability,

prototype stability,

fragmentation reduction.



---

Phase 4

Inject fused embeddings into MSDD.

Evaluate:

DER,

confusion reduction,

cpWER,

WDER,

conversational timeline quality.



---

Phase 5

ONLY if stable: explore:

partial backbone adaptation,

overlap-aware gating,

conversational attention fusion,

end-to-end refinement.



---

Final Research Framing

This work is NOT: “using multiple embeddings.”

This work is:

conversationally stable multi-view speaker representation learning for noisy real-world diarization.

The primary scientific contribution is: joint optimization of:

conversational manifold stability,

prototype consistency,

overlap robustness,

transferable speaker geometry, under noisy BWC conversational conditions. :::
